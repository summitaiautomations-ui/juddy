# Optional: if you prefer to set the token via a file instead of pasting it
# into the dashboard UI, copy this file to ".env" and put your token below.
# The dashboard reads it on startup. Otherwise leave this alone -- you can
# just paste the token into the dashboard once and it'll be remembered.
NOTION_TOKEN=ntn_paste_your_token_here
