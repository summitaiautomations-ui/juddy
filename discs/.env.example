# Add this one line to your existing ~/juddy/.env
# (don't create a separate file — discs reads from the same .env as outreach)

# Get an API key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
