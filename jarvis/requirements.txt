# Jarvis voice-assistant dependencies (macOS, Python 3.9+).
sounddevice>=0.4.6     # microphone capture via PortAudio
numpy>=1.24            # audio buffers / loudness math
openwakeword>=0.6.0    # "Hey Jarvis" wake-word detection
onnxruntime>=1.16      # ONNX backend for openWakeWord (avoids tflite-runtime)
faster-whisper>=1.0.0  # local speech-to-text
