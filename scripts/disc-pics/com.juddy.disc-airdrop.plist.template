<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.juddy.disc-airdrop</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>__REPO__/scripts/disc-pics/airdrop-catalog.sh</string>
  </array>

  <!-- Fire whenever anything lands in Downloads (that's where AirDrop saves). -->
  <key>WatchPaths</key>
  <array>
    <string>/Users/__USER__/Downloads</string>
  </array>

  <!-- Also catch anything that arrived while the job wasn't loaded. -->
  <key>RunAtLoad</key>
  <true/>

  <!-- Batch a burst of AirDropped photos into one run instead of one per file. -->
  <key>ThrottleInterval</key>
  <integer>30</integer>

  <key>EnvironmentVariables</key>
  <dict>
    <key>CLAUDE_BIN</key>
    <string>__CLAUDE_BIN__</string>
    <key>PATH</key>
    <string>/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin</string>
  </dict>
</dict>
</plist>
